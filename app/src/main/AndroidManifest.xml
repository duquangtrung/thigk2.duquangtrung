<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="ThiGK"
        android:theme="@style/Theme.Thigk2duquangtrung">
        <activity
            android:name=".ThongTinActivity"
            android:exported="false" />
        <!-- CHỨC NĂNG 1 -->
        <activity android:name=".ChucNang1Activity" /> <!-- CHỨC NĂNG 2 -->
        <activity android:name=".BaiHatActivity" /> <!-- CHỨC NĂNG 3 -->
        <activity android:name=".DanhNhanActivity" /> <!-- MAIN -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>